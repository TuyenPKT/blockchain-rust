[package]
name = "pkt-sdk"
version = "0.1.0"
edition = "2021"
description = "PKT blockchain SDK — types, helpers, and error types for building on PKTScan"
license = "MIT"

[dependencies]
serde      = { workspace = true }
serde_json = { workspace = true }
hex        = { workspace = true }
